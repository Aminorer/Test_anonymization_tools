# Core dependencies required to run the Streamlit anonymization app
# The code dynamically degrades when optional AI backends are missing,
# so we keep the default requirements light enough for Streamlit Cloud.

# --- Web UI ---
streamlit>=1.28,<2.0
streamlit-aggrid>=0.3.4

# --- Document handling ---
python-docx>=0.8.11
pdfplumber>=0.7.6
PyMuPDF>=1.23.0

# --- Data processing ---
pandas>=1.5,<3.0
numpy>=1.24,<2.0
openpyxl>=3.0.10
rapidfuzz>=3.0.0

# --- Visualisation ---
plotly>=5.15,<6.0
matplotlib>=3.6,<4.0
seaborn>=0.12,<1.0

# --- Utilities ---
requests>=2.28,<3.0
python-dotenv>=1.0,<2.0
psutil>=5.9,<6.0
regex>=2023.0.0
unidecode>=1.3.0
ftfy>=6.1.0
chardet>=5.0.0
cachetools>=5.3.0
joblib>=1.3.0
pydantic>=1.10,<3.0
jsonschema>=4.17,<5.0
loguru>=0.7,<1.0
Pillow>=9.5,<11.0
lxml>=4.9.0
beautifulsoup4>=4.12.0

# --- Optional but lightweight NLP backend ---
spacy>=3.4,<4.0

# For advanced AI features (PyTorch/Transformers/Ollama),
# install the extra file `requirements-full.txt` locally.
